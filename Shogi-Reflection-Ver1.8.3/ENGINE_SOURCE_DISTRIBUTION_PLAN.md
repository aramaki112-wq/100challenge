# ENGINE_SOURCE_DISTRIBUTION_PLAN — Ver.1.8.3 Run #8 Candidate

Date: 2026-08-11
Status: engineering source-delivery plan; **NOT a legal conclusion**

## Current Run #8 patch

The patch does not contain a newly accepted Real YaneuraOu binary. Run #8 keeps the exact official V9.00 commit as the immutable base, applies the documented hash-bound two-file WASM USI bridge patch, and builds the MATERIAL WASM profile with the upstream-compatible Emscripten 3.1.43 toolchain. Both pristine and modified Corresponding Source are retained by CI.

## Exact source identity

If a Real Engine artifact is later conveyed, its recorded engine source identity is:

- repository: official `https://github.com/yaneurao/YaneuraOu`
- release: V9.00
- exact commit: `a5ee2786c0030edc7d4a1cdfe94b04dffec55493`
- selected upstream profile: `material`
- local YaneuraOu source modifications: **none permitted by the Run #6 build script**

## Build/reproduction files

The build evidence retains:

- `.github/workflows/build-yaneuraou-wasm.yml`
- `scripts/build-yaneuraou-wasm.sh`
- `scripts/hash-engine-assets.sh`
- `scripts/update-engine-build-metadata.mjs`
- `scripts/verify-yaneuraou-wasm.sh`
- `scripts/real-yaneuraou-artifact-gate.mjs`
- `real_yaneuraou_usi_verify.py`
- `real_yaneuraou_browser_verify.py`
- `requirements-real-engine.txt`

## Toolchain/profile

- Emscripten/emsdk target: 3.1.43
- official release mapping expected: `bf3c159888633d232c0507f4c76cc156a43c32dc`
- runner label: Ubuntu 22.04
- build entry point: `node script/wasm_build.js material`

The exact pinned upstream `script/wasm_build.js` expands the material build profile. The Bridge stores that upstream script and `.github/workflows/make-wasm.yml` beside the evidence rather than translating the profile into an undocumented custom command.

## Expected generated assets

The pinned upstream material packaging expects:

- `yaneuraou.material.js`
- `yaneuraou.material.worker.js`
- `yaneuraou.material.wasm`

The application also uses the first-party `engine/yaneuraou/YaneuraOuWasmWorkerBootstrap.js`. All four runtime files are independently hash-bound.

## Corresponding Source evidence generated by CI

GitHub Actions creates/retains:

- `corresponding-source/YaneuraOu-a5ee2786c0030edc7d4a1cdfe94b04dffec55493.tar.gz`
- SHA-256 for that exact-commit archive
- pinned upstream README
- pinned `source/Makefile`
- pinned `source/wasm_pre.js`
- pinned `script/wasm_build.js`
- pinned `.github/workflows/make-wasm.yml`
- upstream license file if present
- Emscripten license evidence if present
- compiler/toolchain/environment records
- generated runtime hashes

The source archive is created with `git archive` from the exact pinned commit, separate from generated build output.

## Public distribution decision still required

Before a public binary package is shipped, document a durable method by which recipients can obtain source corresponding to the exact conveyed binary, including at minimum:

1. exact source archive/commit;
2. exact Build Bridge scripts and upstream build profile;
3. toolchain version/provenance;
4. any future local modifications;
5. applicable licenses/notices;
6. stable delivery/availability method and legally required retention period;
7. enough information to reproduce the build or explain unavoidable nondeterminism.

A moving branch URL alone is not accepted as the project's traceability record.

## Application boundary

This engineering plan does not decide whether first-party JavaScript/Worker integration and a GPL engine binary constitute aggregation or a combined/derivative work. WASM, Worker or JavaScript messaging boundaries do not automatically decide that legal question.

**LEGAL REVIEW REQUIRED BEFORE PUBLIC DISTRIBUTION** of a package conveying the Real YaneuraOu engine unless the distribution obligations and combined-work characterization have been conclusively resolved.

## Run #8 modified-source Corresponding Source plan

Run #8 changes the previous “no local YaneuraOu source modifications” assumption. The fixed official commit remains the base, but an accepted Real WASM will include the documented two-file WASM USI bridge patch.

CI must retain together:

- pristine upstream archive: `YaneuraOu-a5ee2786c0030edc7d4a1cdfe94b04dffec55493.tar.gz`;
- reviewed patch: `yaneuraou-v9.00-wasm-usi-bridge.patch`;
- actual applied diff: `YaneuraOu-ShogiReflection-WASM-USI-Bridge.patch`;
- deterministic modified-source archive: `YaneuraOu-a5ee2786c0030edc7d4a1cdfe94b04dffec55493-ShogiReflection-WASM-USI-Bridge.tar.gz`;
- patch and archive SHA-256 records;
- `SOURCE_MODIFICATION_MANIFEST.txt`;
- fixed build script, toolchain version/image digest, build command and compiler evidence;
- YaneuraOu README / `Copying.txt` and applicable Emscripten notices.

The modified-source archive, not only the pristine repository URL, is the source snapshot intended to correspond to a conveyed patched WASM. Public/commercial distribution remains blocked pending final license/legal review and complete runtime evidence.
