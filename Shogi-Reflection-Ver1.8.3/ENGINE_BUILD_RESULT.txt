Shogi Reflection Ver.1.8.3 YaneuraOu WASM Build Result
======================================================
Status: RUN #8 NOT YET EXECUTED — NOT FORMAL
Date: 2026-08-11

Run #7 measured facts:
- YaneuraOu V9.00 fixed commit build completed with make exit code 0.
- Emscripten 3.1.43 produced JS + pthread worker.js + WASM.
- The generated WASM did not export usi_command required by the pinned wasm_pre.js command queue.
- Real Artifact Gate therefore remained closed; Real USI / Real E2E were not accepted.

Run #8 prepared change:
- keep the official V9.00 commit as the immutable source base
- keep the upstream-compatible emscripten/emsdk:3.1.43 toolchain
- apply one explicit, hash-bound, two-file Emscripten-only USI bridge patch
- preserve the current USIEngine API and keep the Emscripten engine/USI objects alive after engine_main() returns
- export usi_command with EMSCRIPTEN_KEEPALIVE
- reject the build unless the resulting WASM physically exports usi_command or _usi_command
- package both pristine upstream source and deterministic modified corresponding source with the exact patch

No Run #8 Real build, Real USI or Real application E2E success is claimed by this placeholder.
